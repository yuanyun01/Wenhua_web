快速启动说明

1. 在项目根目录进入 server 文件夹：

```bash
cd server
```

2. 安装依赖：

```bash
npm install
```

3. 启动服务（开发）：

```bash
npm run dev
```

或直接运行：

```bash
npm start
```

4. 打开浏览器访问：

http://localhost:3000/index.html

说明：该后端会把项目根目录作为静态文件目录，并提供简单的 `/api/events` 与 `/api/login` 接口用于前端联调。